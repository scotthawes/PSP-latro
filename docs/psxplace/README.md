# PSX-Place archives

This folder is for saving fetched PSX-Place forum threads or related research items used during debugging and porting.

Current status: empty. Use `scripts/` helpers to fetch threads into this directory and add a short `.md` summary for each saved thread.

Respect forum terms of service and attribution when saving or sharing content.
